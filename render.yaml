services:
  - type: web
    name: xerf-backend
    runtime: python
    plan: free
    buildCommand: pip install -r backend/requirements.txt
    startCommand: PYTHONPATH=backend uvicorn src.api:app --host 0.0.0.0 --port $PORT
    branch: main
